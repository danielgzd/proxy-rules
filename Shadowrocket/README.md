# Shadowrocket 精简分流

## 使用

1. 导入 `Default.conf`。
2. 把 `[Proxy]` 里的 `MOCK-HYSTERIA2` 替换成自己的节点，或在 App 里单独导入订阅。
3. 规则和策略组保持简洁：海外 AI、流媒体、开发服务、社交媒体、云与软件、普通代理。

## 注意

Shadowrocket 的订阅管理通常在 App 内完成，这份模板不写入真实订阅 URL，只保留 mock 手动节点。
