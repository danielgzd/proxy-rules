# Quantumult X 精简分流

## 使用

1. 导入 `Default.conf`。
2. 替换 `[server_local]` 的 `MOCK-HYSTERIA2`，或替换 `[server_remote]` 的 mock 订阅。
3. 保持 `AI`、`流媒体`、`开发服务`、`社交媒体`、`云与软件` 这些主分流即可。

## 设计

规则来源使用 Blackmatrix7 的 Quantumult X 原生规则，国内 AI 在本地规则先直连。
